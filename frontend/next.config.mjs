/** @type {import('next').NextConfig} */
const nextConfig = {

  // Konfigurasi gambar Anda sudah benar, tidak perlu diubah
  images: {
    domains: ['localhost', '127.0.0.1', 'server.fitmind.my.id', 'fitmind.my.id'],
  },
};

export default nextConfig;